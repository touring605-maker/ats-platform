// TODO(ATS-000): Wire up Express app with middleware stack and product route registration
// This is the application entry point per the Architecture Standards.
// See artifacts/api-server/src/index.ts for the current Express setup.

export {};
